name: Site

on:
  workflow_dispatch:
    inputs:
      run_id:
        type: string
        required: true

jobs:

  doc_publish:
    uses: ./.github/workflows/doc_publish.yml
    with:
      run_id: ${{ inputs.run_id }}
